(() => {
    const translations = {
        pt: {
            page_title: "Nordaris Partners",
            page_description: "Nordaris Partners. Estratégia, dados, inteligência artificial e automação para decisões mais claras.",
            skip: "Ir para o conteúdo",
            nav_about: "Sobre",
            nav_solutions: "Soluções",
            nav_method: "Método",
            nav_contact: "Contato",
            nav_talk: "Iniciar conversa",
            menu_label: "Navegação",
            hero_title: "Tecnologia de ponta para inovação nos seus negócios.",
            hero_cta: "Conheça nossas soluções",
            hero_more: "Descubra a Nordaris",
            about_label: "Sobre a Nordaris",
            about_title: "Clareza para decidir. Estrutura para avançar.",
            about_p1: "A Nordaris Partners combina visão estratégica e profundidade técnica para criar soluções de tecnologia sob medida para cada negócio.",
            about_p2: "Nosso compromisso vai além da entrega. Trabalhamos lado a lado com lideranças e equipes para transformar informação em direção, capacidade e resultado.",
            principle_1_title: "Precisão",
            principle_1_text: "Decisões orientadas por dados.",
            principle_2_title: "Visão",
            principle_2_text: "Tecnologia alinhada ao negócio.",
            principle_3_title: "Solidez",
            principle_3_text: "Soluções confiáveis, seguras e duradouras.",
            solutions_label: "Nossas soluções",
            solutions_title: "Soluções sob medida para impulsionar os negócios.",
            service_1_title: "Consultoria Estratégica",
            service_1_text: "Alinhamos tecnologia, dados e prioridades executivas em um plano claro, com critérios de valor, risco e execução.",
            service_2_title: "Inteligência Artificial",
            service_2_text: "Desenhamos e implementamos soluções de IA aplicadas a problemas concretos, com validação, segurança e integração ao trabalho.",
            service_3_title: "Dados & Business Intelligence",
            service_3_text: "Organizamos dados dispersos em uma camada confiável de gestão, com indicadores, análises e dashboards para decisões contínuas.",
            service_4_title: "Automação de Processos",
            service_4_text: "Conectamos sistemas e automatizamos rotinas críticas para reduzir retrabalho, tempo operacional e falhas manuais.",
            service_more: "Explorar solução",
            solution_offers_label: "O que oferecemos",
            solution_results_label: "Aplicações e resultados",
            solution_contact: "Conversar sobre esta solução",
            solution_close_aria: "Fechar detalhes da solução",
            method_label: "Como trabalhamos",
            method_title: "Da estratégia à execução.",
            method_description: "Entramos no contexto do negócio antes de propor tecnologia. O resultado são soluções relevantes, mensuráveis e orientadas à geração de valor.",
            step_1_title: "Entender",
            step_1_text: "Objetivos, operação, dados e restrições.",
            step_2_title: "Estruturar",
            step_2_text: "Prioridades, arquitetura e critérios de sucesso.",
            step_3_title: "Construir",
            step_3_text: "Soluções integradas, seguras e validadas.",
            step_4_title: "Evoluir",
            step_4_text: "Acompanhamento, aprendizado e escala.",
            north_label: "Por que Nordaris Partners?",
            north_title: "Soluções sofisticadas para novos caminhos.",
            north_p1: "Unimos ciência de dados de alta performance para orientar decisões com mais assertividade.",
            contact_label: "Próximo passo",
            contact_title: "Vamos transformar uma prioridade em resultado.",
            contact_cta: "Fale com a Nordaris",
            assistant_open: "Fale com a Nordaris",
            assistant_name: "Assistente Nordaris",
            assistant_status: "Atendimento inicial",
            assistant_reset_aria: "Reiniciar conversa",
            assistant_close_aria: "Fechar conversa",
            assistant_input_label: "Sua resposta",
            assistant_placeholder: "Escreva sua resposta",
            assistant_send_aria: "Enviar resposta",
            assistant_note: "Mensagem protegida e enviada diretamente à equipe Nordaris.",
            assistant_sending: "Enviando...",
            assistant_sent: "Mensagem recebida. Ela ficou registrada e nossa equipe entrará em contato.",
            assistant_saved_local: "Prévia local: a mensagem ficou salva neste navegador. O envio online funcionará após a publicação.",
            assistant_saved_pending: "A mensagem ficou salva neste dispositivo e será enviada quando o formulário online estiver disponível.",
            assistant_welcome: "Olá. Em que podemos ajudar?",
            assistant_choice_consulting: "Estratégia",
            assistant_choice_ai: "Inteligência Artificial",
            assistant_choice_data: "Dados e BI",
            assistant_choice_automation: "Automação",
            assistant_detail_prompt: "Conte em uma frase o que você precisa alcançar.",
            assistant_contact_prompt: "Como podemos falar com você? Informe seu nome, e-mail ou telefone.",
            assistant_clarify: "Escolha uma área abaixo ou descreva brevemente o que precisa.",
            footer_tagline: "Tecnologia de ponta para inovação nos seus negócios.",
            footer_office: "Escritório",
            menu_open: "Abrir menu",
            menu_close: "Fechar menu",
            nav_aria: "Navegação principal",
            nav_mobile_aria: "Navegação móvel",
            language_aria: "Selecionar idioma",
            hero_image: "Vista urbana de São Paulo"
        },
        en: {
            page_title: "Nordaris Partners",
            page_description: "Nordaris Partners. Strategy, data, artificial intelligence and automation for clearer decisions.",
            skip: "Skip to content",
            nav_about: "About",
            nav_solutions: "Solutions",
            nav_method: "Method",
            nav_contact: "Contact",
            nav_talk: "Start a conversation",
            menu_label: "Navigation",
            hero_title: "Advanced technology to drive innovation in your business.",
            hero_cta: "Explore our solutions",
            hero_more: "Discover Nordaris",
            about_label: "About Nordaris",
            about_title: "Clarity to decide. Structure to move forward.",
            about_p1: "Nordaris Partners combines strategic vision and technical depth to create technology solutions tailored to each business.",
            about_p2: "Our commitment goes beyond delivery. We work alongside leaders and teams to turn information into direction, capability and results.",
            principle_1_title: "Precision",
            principle_1_text: "Data-driven decisions.",
            principle_2_title: "Vision",
            principle_2_text: "Technology aligned with business objectives.",
            principle_3_title: "Strength",
            principle_3_text: "Reliable, secure and lasting solutions.",
            solutions_label: "Our solutions",
            solutions_title: "Tailored solutions to drive business growth.",
            service_1_title: "Strategic Consulting",
            service_1_text: "We align technology, data and executive priorities in a clear plan, with defined criteria for value, risk and execution.",
            service_2_title: "Artificial Intelligence",
            service_2_text: "We design and implement AI solutions for concrete business problems, with validation, security and workflow integration.",
            service_3_title: "Data & Business Intelligence",
            service_3_text: "We organize dispersed data into a trusted management layer, with metrics, analysis and dashboards for continuous decisions.",
            service_4_title: "Process Automation",
            service_4_text: "We connect systems and automate critical routines to reduce rework, operating time and manual errors.",
            service_more: "Explore solution",
            solution_offers_label: "What we offer",
            solution_results_label: "Applications and outcomes",
            solution_contact: "Discuss this solution",
            solution_close_aria: "Close solution details",
            method_label: "How we work",
            method_title: "From strategy to execution.",
            method_description: "We understand the business context before proposing technology. The result is a set of relevant, measurable solutions focused on generating value.",
            step_1_title: "Understand",
            step_1_text: "Goals, operations, data and constraints.",
            step_2_title: "Structure",
            step_2_text: "Priorities, architecture and success criteria.",
            step_3_title: "Build",
            step_3_text: "Integrated, secure and validated solutions.",
            step_4_title: "Evolve",
            step_4_text: "Continuous support, learning and scale.",
            north_label: "Why Nordaris Partners?",
            north_title: "Sophisticated solutions for new paths.",
            north_p1: "We use high-performance data science to guide more assertive decisions.",
            contact_label: "Next step",
            contact_title: "Let us turn one priority into a result.",
            contact_cta: "Talk to Nordaris",
            assistant_open: "Talk to Nordaris",
            assistant_name: "Nordaris Assistant",
            assistant_status: "Initial conversation",
            assistant_reset_aria: "Restart conversation",
            assistant_close_aria: "Close conversation",
            assistant_input_label: "Your answer",
            assistant_placeholder: "Write your answer",
            assistant_send_aria: "Send answer",
            assistant_note: "Protected message sent directly to the Nordaris team.",
            assistant_sending: "Sending...",
            assistant_sent: "Message received. It has been recorded and our team will contact you.",
            assistant_saved_local: "Local preview: the message was saved in this browser. Online delivery will work after publishing.",
            assistant_saved_pending: "The message was saved on this device and will be sent when the online form is available.",
            assistant_welcome: "Hello. How can we help?",
            assistant_choice_consulting: "Strategy",
            assistant_choice_ai: "Artificial Intelligence",
            assistant_choice_data: "Data and BI",
            assistant_choice_automation: "Automation",
            assistant_detail_prompt: "In one sentence, tell us what you need to achieve.",
            assistant_contact_prompt: "How can we reach you? Share your name, email or phone number.",
            assistant_clarify: "Choose an area below or briefly describe what you need.",
            footer_tagline: "Advanced technology to drive innovation in your business.",
            footer_office: "Office",
            menu_open: "Open menu",
            menu_close: "Close menu",
            nav_aria: "Main navigation",
            nav_mobile_aria: "Mobile navigation",
            language_aria: "Select language",
            hero_image: "Urban view of São Paulo"
        }
    };

    const solutionDetails = {
        pt: {
            consulting: {
                title: "Consultoria Estratégica",
                intro: "Transformamos desafios de negócio em prioridades tecnológicas claras, conectando estratégia, dados, processos e investimento.",
                offers: [
                    ["Estratégia e roadmap", "Diagnóstico de maturidade, prioridades e plano de evolução com metas e responsabilidades."],
                    ["Arquitetura e governança", "Diretrizes para sistemas, dados, segurança e decisões técnicas alinhadas ao negócio."],
                    ["Descoberta de produtos", "Estruturação e validação de produtos digitais antes de investimentos de maior escala."],
                    ["Apoio executivo", "Análises independentes para decisões críticas de tecnologia."]
                ],
                results: ["Prioridades claras para investir", "Menos risco e retrabalho", "Tecnologia conectada à estratégia"]
            },
            ai: {
                title: "Inteligência Artificial",
                intro: "Aplicamos inteligência artificial a decisões, produtos e operações a partir de problemas concretos e resultados mensuráveis.",
                offers: [
                    ["IA generativa e assistentes", "Assistentes, copilotos e chatbots conectados ao conhecimento e aos fluxos da empresa."],
                    ["Machine Learning", "Modelos preditivos para antecipar demanda, comportamento, risco, falhas e oportunidades."],
                    ["NLP e inteligência documental", "Classificação, extração, busca e síntese de informações em textos e documentos."],
                    ["Visão computacional", "Análise automatizada de imagens e vídeos para inspeção, classificação e monitoramento."],
                    ["MLOps e governança", "Implantação, segurança e monitoramento contínuo de modelos em produção."]
                ],
                results: ["Previsões e recomendações", "Detecção de anomalias e fraudes", "Decisões apoiadas por IA", "Modelos seguros e monitorados"]
            },
            data: {
                title: "Dados e Business Intelligence",
                intro: "Criamos uma base confiável para transformar dados dispersos em indicadores claros, análises consistentes e decisões recorrentes.",
                offers: [
                    ["Plataformas de dados", "Data warehouses, lakehouses, pipelines e integrações preparados para escala e governança."],
                    ["Dashboards e BI", "Painéis executivos e operacionais com indicadores relevantes, rastreáveis e fáceis de usar."],
                    ["Analytics avançado", "Segmentações, projeções, análises de cenário e identificação de padrões de negócio."],
                    ["Qualidade e governança", "Regras, catálogo, linhagem, acessos e monitoramento para aumentar a confiança nos dados."]
                ],
                results: ["Visão integrada do negócio", "Indicadores confiáveis", "Menos relatórios manuais", "Mais autonomia para analisar"]
            },
            automation: {
                title: "Automação de Processos",
                intro: "Redesenhamos rotinas e conectamos sistemas para reduzir esforço manual, erros e tempo de resposta em operações críticas.",
                offers: [
                    ["Mapeamento de processos", "Análise de gargalos, riscos e oportunidades antes de automatizar."],
                    ["Workflows e RPA", "Automação de tarefas repetitivas, aprovações, documentos e rotinas administrativas."],
                    ["Integrações e APIs", "Conexão entre sistemas, plataformas e fontes de dados sem duplicação de trabalho."],
                    ["Automação inteligente", "Uso de IA para interpretar documentos, classificar solicitações e apoiar decisões."],
                    ["Monitoramento operacional", "Indicadores e alertas para acompanhar execução, falhas e ganhos de eficiência."]
                ],
                results: ["Menos retrabalho e falhas", "Processos rápidos e rastreáveis", "Sistemas integrados", "Mais foco em atividades de valor"]
            }
        },
        en: {
            consulting: {
                title: "Strategic Consulting",
                intro: "We turn business challenges into clear technology priorities, connecting strategy, data, processes and investment.",
                offers: [
                    ["Strategy and roadmap", "Maturity assessment, priorities and an evolution plan with clear goals and ownership."],
                    ["Architecture and governance", "Guidelines for systems, data, security and technical decisions aligned with the business."],
                    ["Product discovery", "Structuring and validating digital products before larger investments."],
                    ["Executive advisory", "Independent analysis for critical technology decisions."]
                ],
                results: ["Clear investment priorities", "Lower risk and rework", "Technology connected to strategy"]
            },
            ai: {
                title: "Artificial Intelligence",
                intro: "We apply artificial intelligence to decisions, products and operations through concrete problems and measurable outcomes.",
                offers: [
                    ["Generative AI and assistants", "Assistants, copilots and chatbots connected to company knowledge and workflows."],
                    ["Machine Learning", "Predictive models for demand, behavior, risk, failures and opportunities."],
                    ["NLP and document intelligence", "Classification, extraction, search and synthesis across text and documents."],
                    ["Computer vision", "Automated image and video analysis for inspection, classification and monitoring."],
                    ["MLOps and governance", "Deployment, security and continuous monitoring for production models."]
                ],
                results: ["Forecasts and recommendations", "Anomaly and fraud detection", "AI-supported decisions", "Secure and monitored models"]
            },
            data: {
                title: "Data and Business Intelligence",
                intro: "We create a trusted foundation that turns dispersed data into clear metrics, consistent analysis and recurring decisions.",
                offers: [
                    ["Data platforms", "Data warehouses, lakehouses, pipelines and integrations designed for scale and governance."],
                    ["Dashboards and BI", "Executive and operational dashboards with relevant, traceable and accessible metrics."],
                    ["Advanced analytics", "Segmentation, forecasts, scenario analysis and discovery of business patterns."],
                    ["Quality and governance", "Rules, catalog, lineage, access and monitoring that increase trust in data."]
                ],
                results: ["An integrated business view", "Reliable metrics", "Less manual reporting", "More analytical autonomy"]
            },
            automation: {
                title: "Process Automation",
                intro: "We redesign routines and connect systems to reduce manual effort, errors and response time in critical operations.",
                offers: [
                    ["Process mapping", "Analysis of bottlenecks, risks and opportunities before automation."],
                    ["Workflows and RPA", "Automation of repetitive tasks, approvals, documents and administrative routines."],
                    ["Integrations and APIs", "Connections across systems, platforms and data sources without duplicated work."],
                    ["Intelligent automation", "AI applied to documents, request classification and operational decisions."],
                    ["Operational monitoring", "Metrics and alerts to track execution, failures and efficiency gains."]
                ],
                results: ["Less rework and fewer errors", "Fast and traceable processes", "Integrated systems", "More focus on high-value work"]
            }
        }
    };

    const root = document.documentElement;
    const header = document.querySelector("[data-header]");
    const menu = document.querySelector("#mobile-menu");
    const menuToggle = document.querySelector(".menu-toggle");
    const languageButtons = document.querySelectorAll("[data-language]");
    let resetAssistant;
    let renderActiveSolution;

    const setLanguage = (language) => {
        const selected = translations[language] ? language : "pt";
        const copy = translations[selected];

        document.querySelectorAll("[data-i18n]").forEach((element) => {
            const value = copy[element.dataset.i18n];
            if (value) {
                element.textContent = value;
            }
        });

        document.querySelectorAll("[data-i18n-placeholder]").forEach((element) => {
            const value = copy[element.dataset.i18nPlaceholder];
            if (value) element.setAttribute("placeholder", value);
        });

        document.querySelectorAll("[data-i18n-aria]").forEach((element) => {
            const value = copy[element.dataset.i18nAria];
            if (value) element.setAttribute("aria-label", value);
        });

        root.lang = selected === "pt" ? "pt-BR" : "en";
        document.title = copy.page_title;

        const description = document.querySelector('meta[name="description"]');
        if (description) {
            description.setAttribute("content", copy.page_description);
        }

        menuToggle.setAttribute("aria-label", copy.menu_open);
        document.querySelector(".desktop-nav").setAttribute("aria-label", copy.nav_aria);
        document.querySelector(".mobile-menu nav").setAttribute("aria-label", copy.nav_mobile_aria);
        document.querySelector(".language-switch").setAttribute("aria-label", copy.language_aria);
        document.querySelector(".hero-image").setAttribute("aria-label", copy.hero_image);

        languageButtons.forEach((button) => {
            const isActive = button.dataset.language === selected;
            button.classList.toggle("is-active", isActive);
            button.setAttribute("aria-pressed", String(isActive));
        });

        if (resetAssistant) resetAssistant();
        if (renderActiveSolution) renderActiveSolution();

        try {
            localStorage.setItem("nordaris-language", selected);
        } catch (error) {
            // The language still changes when storage is unavailable.
        }
    };

    const closeMenu = () => {
        menu.classList.remove("is-open");
        header.classList.remove("is-menu-open");
        document.body.classList.remove("menu-open");
        menu.setAttribute("aria-hidden", "true");
        menuToggle.setAttribute("aria-expanded", "false");

        const currentLanguage = root.lang === "en" ? "en" : "pt";
        menuToggle.setAttribute("aria-label", translations[currentLanguage].menu_open);
    };

    const openMenu = () => {
        menu.classList.add("is-open");
        header.classList.add("is-menu-open");
        document.body.classList.add("menu-open");
        menu.setAttribute("aria-hidden", "false");
        menuToggle.setAttribute("aria-expanded", "true");

        const currentLanguage = root.lang === "en" ? "en" : "pt";
        menuToggle.setAttribute("aria-label", translations[currentLanguage].menu_close);
    };

    const updateHeader = () => {
        header.classList.toggle("is-scrolled", window.scrollY > 28);
    };

    menuToggle.addEventListener("click", () => {
        const isOpen = menuToggle.getAttribute("aria-expanded") === "true";
        isOpen ? closeMenu() : openMenu();
    });

    menu.querySelectorAll('a[href^="#"]').forEach((link) => {
        link.addEventListener("click", closeMenu);
    });

    document.addEventListener("keydown", (event) => {
        if (event.key === "Escape" && menu.classList.contains("is-open")) {
            closeMenu();
            menuToggle.focus();
        }
    });

    languageButtons.forEach((button) => {
        button.addEventListener("click", () => setLanguage(button.dataset.language));
    });

    const navLinks = document.querySelectorAll('.desktop-nav a[href^="#"]');
    const observedSections = document.querySelectorAll("main section[id]");

    if ("IntersectionObserver" in window) {
        const observer = new IntersectionObserver((entries) => {
            entries.forEach((entry) => {
                if (!entry.isIntersecting) return;
                navLinks.forEach((link) => {
                    link.classList.toggle("is-active", link.hash === "#" + entry.target.id);
                });
            });
        }, { rootMargin: "-32% 0px -58%", threshold: 0 });

        observedSections.forEach((section) => observer.observe(section));
    }

    const compassGraphic = document.querySelector("[data-compass]");

    if (compassGraphic) {
        const reducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;

        if ("IntersectionObserver" in window) {
            const compassObserver = new IntersectionObserver((entries) => {
                entries.forEach((entry) => {
                    compassGraphic.classList.toggle("is-active", entry.isIntersecting);
                });
            }, { threshold: 0.24 });

            compassObserver.observe(compassGraphic);
        } else {
            compassGraphic.classList.add("is-active");
        }

        if (!reducedMotion) {
            let compassFrame;

            compassGraphic.addEventListener("pointermove", (event) => {
                const bounds = compassGraphic.getBoundingClientRect();
                const horizontal = (event.clientX - bounds.left) / bounds.width - 0.5;
                const vertical = (event.clientY - bounds.top) / bounds.height - 0.5;

                cancelAnimationFrame(compassFrame);
                compassFrame = requestAnimationFrame(() => {
                    compassGraphic.style.setProperty("--compass-x", vertical * -5 + "deg");
                    compassGraphic.style.setProperty("--compass-y", horizontal * 5 + "deg");
                });
            });

            compassGraphic.addEventListener("pointerleave", () => {
                compassGraphic.style.setProperty("--compass-x", "0deg");
                compassGraphic.style.setProperty("--compass-y", "0deg");
            });
        }
    }

    const assistant = document.querySelector("[data-assistant]");

    if (assistant) {
        const backdrop = document.querySelector("[data-assistant-backdrop]");
        const openButtons = document.querySelectorAll("[data-assistant-open]");
        const closeButton = assistant.querySelector("[data-assistant-close]");
        const messages = assistant.querySelector("[data-assistant-messages]");
        const actions = assistant.querySelector("[data-assistant-actions]");
        const form = assistant.querySelector("[data-assistant-form]");
        const input = assistant.querySelector("[data-assistant-input]");
        const resetButton = assistant.querySelector("[data-assistant-reset]");
        const state = { step: "service", service: "", challenge: "", contact: "" };

        const language = () => root.lang === "en" ? "en" : "pt";
        const copy = (key) => translations[language()][key];

        const openAssistant = () => {
            backdrop.hidden = false;
            assistant.setAttribute("aria-hidden", "false");
            requestAnimationFrame(() => backdrop.classList.add("is-open"));
            window.setTimeout(() => input.focus(), 220);
        };

        const closeAssistant = () => {
            backdrop.classList.remove("is-open");
            assistant.setAttribute("aria-hidden", "true");
            window.setTimeout(() => {
                backdrop.hidden = true;
            }, 220);
        };

        openButtons.forEach((button) => button.addEventListener("click", openAssistant));
        closeButton.addEventListener("click", closeAssistant);
        backdrop.addEventListener("click", (event) => {
            if (event.target === backdrop) closeAssistant();
        });
        document.addEventListener("keydown", (event) => {
            if (event.key === "Escape" && !backdrop.hidden) closeAssistant();
        });

        const addMessage = (textValue, sender) => {
            const message = document.createElement("p");
            message.className = "assistant-message is-" + sender;
            message.textContent = textValue;
            messages.appendChild(message);
            messages.scrollTop = messages.scrollHeight;
        };

        const choiceSets = {
            service: [
                ["consulting", "assistant_choice_consulting"],
                ["ai", "assistant_choice_ai"],
                ["data", "assistant_choice_data"],
                ["automation", "assistant_choice_automation"]
            ]
        };

        const renderChoices = (kind) => {
            actions.replaceChildren();
            choiceSets[kind].forEach(([value, key]) => {
                const button = document.createElement("button");
                button.type = "button";
                button.dataset.assistantChoice = value;
                button.dataset.assistantKind = kind;
                button.textContent = copy(key);
                actions.appendChild(button);
            });
        };

        const submitLead = async () => {
            if (state.step === "sending") {
                const payload = new URLSearchParams();
                payload.append("form-name", "nordaris-contact");
                payload.append("bot-field", "");
                payload.append("area", state.service);
                payload.append("necessidade", state.challenge);
                payload.append("contato", state.contact);
                payload.append("idioma", language());

                input.disabled = true;
                addMessage(copy("assistant_sending"), "bot");

                const savePending = () => {
                    try {
                        localStorage.setItem("nordaris-pending-message", payload.toString());
                    } catch (error) {
                        // The conversation remains visible even when storage is unavailable.
                    }
                };

                const isLocalPreview = window.location.protocol === "file:"
                    || window.location.hostname === "localhost"
                    || window.location.hostname === "127.0.0.1";

                if (isLocalPreview) {
                    savePending();
                    form.hidden = true;
                    addMessage(copy("assistant_saved_local"), "bot");
                    state.step = "sent";
                    return;
                }

                try {
                    const response = await fetch("/", {
                        method: "POST",
                        headers: { "Content-Type": "application/x-www-form-urlencoded" },
                        body: payload.toString()
                    });
                    if (response.ok === false) throw new Error("Submission failed");

                    form.hidden = true;
                    addMessage(copy("assistant_sent"), "bot");
                    state.step = "sent";
                    try {
                        localStorage.removeItem("nordaris-pending-message");
                    } catch (error) {
                        // No local message was stored.
                    }
                } catch (error) {
                    savePending();
                    form.hidden = true;
                    addMessage(copy("assistant_saved_pending"), "bot");
                    state.step = "sent";
                }
            }
        };

        const selectService = (label) => {
            state.service = label;
            state.step = "detail";
            actions.replaceChildren();
            addMessage(copy("assistant_detail_prompt"), "bot");
            input.focus();
        };

        const inferService = (value) => {
            const normalized = value.toLocaleLowerCase();
            const matches = [
                { pattern: /intelig|\bia\b|\bai\b|chatbot|modelo/, key: "assistant_choice_ai" },
                { pattern: /dado|data|dashboard|indicador|analytics|\bbi\b/, key: "assistant_choice_data" },
                { pattern: /automa|processo|rotina|integra/, key: "assistant_choice_automation" },
                { pattern: /estrat|consult|governan|roteiro|roadmap/, key: "assistant_choice_consulting" }
            ];
            const match = matches.find((item) => item.pattern.test(normalized));
            return match ? copy(match.key) : "";
        };

        actions.addEventListener("click", (event) => {
            const button = event.target.closest("[data-assistant-choice]");
            if (!button) return;

            addMessage(button.textContent, "user");
            selectService(button.textContent);
        });

        form.addEventListener("submit", (event) => {
            event.preventDefault();
            const value = input.value.trim();
            if (!value) return;

            addMessage(value, "user");
            input.value = "";

            if (state.step === "service") {
                const inferred = inferService(value);
                if (inferred) {
                    selectService(inferred);
                } else {
                    addMessage(copy("assistant_clarify"), "bot");
                    renderChoices("service");
                }
                return;
            }

            if (state.step === "detail") {
                state.challenge = value;
                state.step = "contact";
                addMessage(copy("assistant_contact_prompt"), "bot");
                return;
            }

            state.contact = value;
            state.step = "sending";
            submitLead();
        });

        input.addEventListener("keydown", (event) => {
            if (event.key === "Enter" && !event.shiftKey) {
                event.preventDefault();
                form.requestSubmit();
            }
        });

        resetAssistant = () => {
            Object.assign(state, { step: "service", service: "", challenge: "", contact: "" });
            messages.replaceChildren();
            actions.replaceChildren();
            form.hidden = false;
            input.disabled = false;
            input.value = "";
            addMessage(copy("assistant_welcome"), "bot");
            renderChoices("service");
        };

        const retryStoredLead = async () => {
            const isLocalPreview = window.location.protocol === "file:"
                || window.location.hostname === "localhost"
                || window.location.hostname === "127.0.0.1";
            if (isLocalPreview) return;

            try {
                const pendingMessage = localStorage.getItem("nordaris-pending-message");
                if (pendingMessage === null) return;
                const response = await fetch("/", {
                    method: "POST",
                    headers: { "Content-Type": "application/x-www-form-urlencoded" },
                    body: pendingMessage
                });
                if (response.ok) localStorage.removeItem("nordaris-pending-message");
            } catch (error) {
                // The message stays queued for the next connection attempt.
            }
        };

        resetButton.addEventListener("click", resetAssistant);
        window.addEventListener("online", retryStoredLead);
        retryStoredLead();
    }

    const solutionBackdrop = document.querySelector("[data-solution-backdrop]");
    const solutionDialog = document.querySelector("[data-solution-dialog]");

    if (solutionBackdrop && solutionDialog) {
        const title = solutionDialog.querySelector("[data-solution-title]");
        const intro = solutionDialog.querySelector("[data-solution-intro]");
        const offers = solutionDialog.querySelector("[data-solution-offers]");
        const results = solutionDialog.querySelector("[data-solution-results]");
        const closeButton = solutionDialog.querySelector("[data-solution-close]");
        const contactButton = solutionDialog.querySelector("[data-solution-contact]");
        let activeSolution = "";

        renderActiveSolution = () => {
            if (activeSolution === "") return;
            const language = root.lang === "en" ? "en" : "pt";
            const detail = solutionDetails[language][activeSolution];
            if (detail === undefined) return;
            title.textContent = detail.title;
            intro.textContent = detail.intro;
            offers.replaceChildren();
            results.replaceChildren();

            detail.offers.forEach(([offerTitle, offerText]) => {
                const item = document.createElement("article");
                const heading = document.createElement("h4");
                const textValue = document.createElement("p");
                heading.textContent = offerTitle;
                textValue.textContent = offerText;
                item.append(heading, textValue);
                offers.appendChild(item);
            });

            detail.results.forEach((result) => {
                const item = document.createElement("li");
                item.textContent = result;
                results.appendChild(item);
            });
        };

        const openSolution = (key) => {
            activeSolution = key;
            renderActiveSolution();
            solutionBackdrop.hidden = false;
            solutionDialog.setAttribute("aria-hidden", "false");
            document.body.classList.add("solution-dialog-open");
            requestAnimationFrame(() => solutionBackdrop.classList.add("is-open"));
            window.setTimeout(() => closeButton.focus(), 220);
        };

        const closeSolution = () => {
            solutionBackdrop.classList.remove("is-open");
            solutionDialog.setAttribute("aria-hidden", "true");
            document.body.classList.remove("solution-dialog-open");
            window.setTimeout(() => {
                solutionBackdrop.hidden = true;
            }, 220);
        };

        document.querySelectorAll("[data-solution-open]").forEach((button) => {
            button.addEventListener("click", () => openSolution(button.dataset.solutionOpen));
        });
        document.querySelectorAll(".solution-card").forEach((card) => {
            card.addEventListener("click", (event) => {
                if (event.target.closest("[data-solution-open]")) return;
                const trigger = card.querySelector("[data-solution-open]");
                if (trigger) trigger.click();
            });
        });
        closeButton.addEventListener("click", closeSolution);
        contactButton.addEventListener("click", closeSolution);
        solutionBackdrop.addEventListener("click", (event) => {
            if (event.target === solutionBackdrop) closeSolution();
        });
        document.addEventListener("keydown", (event) => {
            if (event.key === "Escape" && solutionBackdrop.hidden === false) closeSolution();
        });
    }

    const requestedLanguage = new URLSearchParams(window.location.search).get("lang");
    let savedLanguage = "pt";
    try {
        savedLanguage = localStorage.getItem("nordaris-language") || "pt";
    } catch (error) {
        savedLanguage = "pt";
    }

    const initialLanguage = requestedLanguage === "en" || requestedLanguage === "pt"
        ? requestedLanguage
        : savedLanguage;

    setLanguage(initialLanguage);
    updateHeader();
    window.addEventListener("scroll", updateHeader, { passive: true });
})();
